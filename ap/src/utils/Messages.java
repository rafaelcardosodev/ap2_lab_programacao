package utils;

public interface Messages {
    String showMessage(String value, Boolean validation);
    Integer showMessage(Integer value, Boolean validation);
    Double showMessage(Double value, Boolean validation);
    Float showMessage(Float value, Boolean validation);
    Character showMessage(Character value, Boolean validation);

}
